import random
import numpy as np
import pandas as pd


def main():
    # General Parameters 
    population_size = 50   # Population size  

    # Solution Boundary 
    lower_bound = -30   # Lower bound of the search space
    upper_bound = 30   # Upper bound of the search space
    runs = 30   # Number of runs to perform the Genetic Algorithm
    
    # Parameter for Mutation
    Beta = 0.5   # Beta value for the selection function
    Gama = 0.5   # Gama value for the selection function

    dim = 2   # Dimension of the problem
    for run in runs: 
        # Create the initial population
        population = np.random.uniform(low=lower_bound, high=upper_bound, size=(population_size, dim))
        
        # Create Fitness function: function for each function to be optimized
        
        # Create Mutation function

        # Create Selection function



if __name__ == "__main__":
    main()